{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd /app && yarn install && npx prisma generate"
  },
  "deploy": {
    "startCommand": "cd /app && npx prisma migrate deploy && cd apps/api && npx ts-node --transpile-only -r tsconfig-paths/register src/index.ts",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
