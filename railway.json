{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd /app && yarn install && yarn workspace @adminhub/shared build && yarn workspace @adminhub/api build && npx prisma generate"
  },
  "deploy": {
    "startCommand": "cd /app && npx prisma migrate deploy && node apps/api/dist/index.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
