{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd /app && yarn install && cd apps/api && npx prisma generate"
  },
  "deploy": {
    "startCommand": "cd /app/apps/api && npx prisma migrate deploy && npx ts-node --transpile-only -r tsconfig-paths/register src/index.ts",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
